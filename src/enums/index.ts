export enum HealthAPIStatus {
	SUCCESS = 'healthy',
}

/** What a piece of feedback is about — drives the Discord post's heading and colour. */
export enum FeedbackCategory {
	BUG = 'bug',
	FEATURE = 'feature',
	AUDIO = 'audio',
	GENERAL = 'general',
}
